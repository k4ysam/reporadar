[project]
name = "reporadar"
version = "0.1.0"
description = "Automated GitHub discovery newsletter pipeline"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["src*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
